services:
  mysql:
    image: mysql:8.0
    env_file:
      - ../.env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ../db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  licencias:
    build:
      context: ../services/licencias
      dockerfile: Dockerfile
    env_file:
      - ../.env
    ports:
      - "${LICENCIAS_PORT}:3001"
    environment:
      - PORT=3001
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASSWORD}
      - DB_NAME=${MYSQL_DATABASE}
      - DB_PORT=3306
    depends_on:
      mysql:
        condition: service_healthy

  portal-paciente:
    build:
      context: ../services/pacientes
      dockerfile: Dockerfile
    env_file:
      - ../.env
    ports:
      - "${PORTAL_PACIENTE_PORT}:${PORTAL_PACIENTE_PORT}"
    environment:
      - PORTAL_PACIENTE_PORT=${PORTAL_PACIENTE_PORT}
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - PORTAL_PACIENTE_DB=${PORTAL_PACIENTE_DB}
      - DB_PORT=3306
      - LICENCIAS_URL=http://licencias:3001
    depends_on:
      mysql:
        condition: service_healthy
      licencias:
        condition: service_started

  validador-aseguradora:
    build:
      context: ../services/doctores
      dockerfile: Dockerfile
    env_file:
      - ../.env
    ports:
      - "${VALIDADOR_ASEGURADORA_PORT}:${VALIDADOR_ASEGURADORA_PORT}"
    environment:
      - VALIDADOR_ASEGURADORA_PORT=${VALIDADOR_ASEGURADORA_PORT}
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - VALIDADOR_ASEGURADORA_DB=${VALIDADOR_ASEGURADORA_DB}
      - DB_PORT=3306
      - LICENCIAS_URL=http://licencias:3001
    depends_on:
      mysql:
        condition: service_healthy
      licencias:
        condition: service_started

  consumer-tests:
    build:
      context: ../consumer-tests
      dockerfile: Dockerfile.consumer-tests
    env_file:
      - ../.env
    environment:
      - PORTAL_PACIENTE_URL=http://portal-paciente:${PORTAL_PACIENTE_PORT}
      - VALIDADOR_ASEGURADORA_URL=http://validador-aseguradora:${VALIDADOR_ASEGURADORA_PORT}
      - LICENCIAS_URL=http://licencias:3001
    depends_on:
      - portal-paciente
      - validador-aseguradora
      - licencias

volumes:
  mysql_data:
